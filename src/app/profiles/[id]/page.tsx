import Link from "next/link";
export default async function ProfileDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <section className="page"><div className="page-head"><div className="eyebrow">Plan individual</div><h1>Apoyos y planes conocidos.</h1><p>Este perfil nunca autoriza una estrategia rechazada ni reemplaza el estado actual.</p></div><div className="grid"><div className="card"><h2>Apoyos aceptados y rechazados</h2><p className="muted">Administra los apoyos mediante la API segura del perfil. La interfaz detallada se habilita al cargar el perfil autenticado.</p><code>{id}</code></div><div className="card"><h2>Plan profesional</h2><p className="muted">NEXO distingue el texto del cuidador, el validado por profesional y las recomendaciones generadas.</p><Link className="button button-secondary" href="/profiles">Volver a perfiles</Link></div></div></section>; }
