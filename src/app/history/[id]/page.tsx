import Link from "next/link";
export default async function HistoryDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <section className="page"><div className="page-head"><div className="eyebrow">Detalle protegido</div><h1>Sesión guardada.</h1><p>Los eventos técnicos conservan estados y banderas normalizadas; no exponen la narrativa en logs.</p></div><div className="card"><code>{id}</code><div className="actions"><Link className="button button-secondary" href="/history">Volver</Link><Link className="button button-primary" href="/postcrisis">Preparar documento</Link></div></div></section>; }
